spring:
  application:
    name: order
  cloud:
    alibaba:
      seata:
        tx-service-group: order_tx_group
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        group: SEATA_GROUP
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/seata_order?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8
    username: root
    password: 123456

server:
  port: 8083


mybatis-plus:
  type-aliases-package: com.wss.order.entity
  mapper-locations:
    - classpath:/mapper/*Mapper.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.wss.order.mapper: DEBUG